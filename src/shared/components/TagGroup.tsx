/**
 * TagGroup
 * - 단일 선택 태그 칩 그룹. TagButton 을 감싸 options 중 하나를 선택한다.
 * - 폐기 카테고리 / 공지 태그 등 글쓰기 폼에서 공용으로 사용하는 headless UI.
 */

const TagGroup = () => {
  // TODO: options / value / onChange 기반 단일 선택 구현
  return null;
};

export default TagGroup;
