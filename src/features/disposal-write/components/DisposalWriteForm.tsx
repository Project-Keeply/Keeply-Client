/**
 * 폐기관리 글쓰기 폼 (뷰)
 * - use-disposal-write-form 훅의 상태를 받아 shared 원자 컴포넌트
 *   (Input · TagGroup · ImgUploadButton 등)를 WritePageLayout 위에 조립한다.
 * - 폼 로직은 갖지 않는 순수 뷰. (상태/제출은 훅 담당)
 */

const DisposalWriteForm = () => {
  // TODO: 훅 연결 및 필드 조립
  return null;
};

export default DisposalWriteForm;
